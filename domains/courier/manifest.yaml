# Картридж courier — метаданные для domain_runtime catalog / Accept.
cartridge_type: courier
version: "0.1.0"
entry: domains.courier.processes:register_all

# Сверять guards/effects только для зарегистрированных ProcessDef.
graph_scope: registered_processes

required_modules:
  - processes
  - commands
  - queries
  - guards
  - effects
  - context
  - db_layer

required_tables:
  - orders
  - stage_orders
  - locker_cells
  - lockers
  - cell_access_tokens
  - users

required_routines: []
