# URL платформенного API (uvicorn main:app).
# Для локального ЛК на http://localhost:3000 используйте http://localhost:8000
# (не 127.0.0.1) — иначе Chrome Private Network Access режет запросы.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
